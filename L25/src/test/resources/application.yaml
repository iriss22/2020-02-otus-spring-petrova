spring:
  shell:
    interactive:
      enabled: false

  datasource:
    initialization-mode: always

  jpa:
    generate-ddl: false
    #generate-ddl: true
    hibernate:
      ddl-auto: none
      #ddl-auto: create-drop

    show-sql: true